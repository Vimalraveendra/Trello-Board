version: 2

jobs:
  build:
    docker:
      - image: circleci/node:10.16.3
    steps:
      - checkout
      - run: echo "yarn installing"
      - run: yarn install
      - run: CI=true  yarn run build
  test:
    docker:
      - image: circleci/node:10.16.3
    steps:
      - checkout
      - run: echo "testing stuff"
      - run: yarn install
      - run: yarn test
  hello_test:
    docker:
      - image: circleci/node:10.16.3
    steps:
      - checkout
      - run: echo "helloooo!"

workflows:
  version: 2
  build_test_and_lint:
    jobs:
      - hello_test
      - build:
          requires:
            - hello_test
